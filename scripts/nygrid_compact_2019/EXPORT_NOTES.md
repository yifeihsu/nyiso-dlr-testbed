The main historical comparison uses the **51-bus pre-2025 compact topology**: 92 branch records, 87 active branches, and 35 inherited generator records. The current 71-bus late-2025 model remains unchanged. This historical comparison network is an NPCC research approximation; it is not a verified as-built 2019 model.

Run `out = export_compact_network` after adding this directory to the MATLAB path. No PF or optimization is performed. The export rebuilds the gross compact construction instead of loading the old boundary-adjusted reference. Fixed public-to-benchmark scaling is `gamma2019 = 0.358662225381536`. Multiply public active powers and comparison targets by gamma once; the exported network already uses benchmark MW on a 100-MVA electrical base.

`compact_network.mat` contains native MATLAB `out` and a plain numeric/string `network_data` struct readable with SciPy `loadmat(..., simplify_cells=True)`. Each of `parent`, `paper74`, and `physical77` contains `bus`, `gen`, `branch`, `gencost`, bus names, and stable device keys. Full current and legacy interface coefficient matrices are included. For AC use `Cf*PF + Ct*PT`; only for lossless DC use `(Cf-Ct)*PF`. Membership comes from bus roles and checked circuit identities, with retired paths excluded. These are model proxies, not verified public metering definitions.

Both 2019 fleet variants append Indian Point 2/3 comparison records using the released NYgrid public capacities 1025.9/1039.9 MW. `paper74` retains the release's allocation terminal and is the literal transfer comparison. `physical77` tests the compact Buchanan interpretation separately. Neither relabels buses. The CSV metadata preserves paper allocation zones and compact physical zones independently. Added Q limits are finite symmetric assumptions corresponding to 0.95 power factor at Pmax, not observed capability curves. Pmin is zero, without an implied nuclear must-run constraint. The compact native reference remains bus42; any transfer experiment changing slack policy must account for that explicitly.

`current_input_snapshots.mat` contains the five previously qualified 2019 source-input hours. Native `selected_inputs{k}.paper74` and `.physical77` have the schema accepted by `fit_compact_nyiso_operating_snapshot`, with `fit_interfaces=false`; `fit_compact_partial_spc_operating_snapshot` offers stricter IPOPT convergence controls while retaining the same objective and physical audits. The plain `input_data` preserves the numeric cases, prior vectors, gross P/Q, fixed boundary P/Q, and legacy raw/bounded/solved generation separately.

The previously unallocated **combined Zone-H source total** is apportioned by capacity between the two nuclear comparison proxies. This is an explicit allocation approximation, not a plant-specific observed nuclear dispatch. All raw source priors are retained without clipping, including January/April assignments above the declared proxy Pmax. The bounded fitter must report any dispatch movement needed to become feasible. Interface targets in its prepared adapter are NaN and forbidden in the objective. No new AC qualification is claimed by export.

The source input campaign used hourly forward hold with a 600-second maximum gap for P32. NYgrid used an arithmetic mean of samples. These methods must stay labelled separately when comparing inputs; use one consistently chosen observation series for scoring. Export source manifests fingerprint the original evidence files; separate output manifests fingerprint the exported artifacts. CSVs use LF endings.
